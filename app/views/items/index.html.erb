<h1>All The Nineties Things</h1>

<div>
  <%= link_to "Add a new Thing", new_item_path %>
</div>

<div>
  <% @items.each do |item| %>
    <div>
      <h3><%= link_to item.title, item %></h3>
    </div>
  <% end %>
</div>
